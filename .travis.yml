language: php
php:
  - '5.6'
  - '7.0'
  - '7.1'

before_install:
    - echo "extension = mongodb.so" >> ~/.phpenv/versions/7.0/etc/php.ini
    - echo "extension = mongodb.so" >> ~/.phpenv/versions/7.1/etc/php.ini
    - echo "extension = mongo.so" >> ~/.phpenv/versions/5.6/etc/php.ini
    - composer install
script: phpunit test